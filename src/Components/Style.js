import {Theme} from './commonComp/Color';

export const error = {
  color: 'rgba(255,0,0,1)',
  fontWeight: '600',
  marginTop: -10,
  marginBottom: 5,
  fontSize: 12,
};

export const Button = {
  color: Theme.PRIMARY,
  borderWidth: 2,
  borderColor: Theme.PRIMARY,
};
